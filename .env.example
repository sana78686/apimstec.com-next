# ─── Local development (copy to .env.local) ───────────────────────────────

# Laravel / GlobalCMS API (no trailing slash)
NEXT_PUBLIC_CMS_API_URL=https://app.apimstec.com

# Tenant hostname in CMS (Domains → domain), without protocol
NEXT_PUBLIC_SITE_DOMAIN=apimstec.com

# Canonical public URL: scheme + host, NO trailing slash.
NEXT_PUBLIC_SITE_ORIGIN=https://www.apimstec.com

# NEXT_PUBLIC_CMS_API_DOMAIN_PATH=true

# ─── Contact form email (Next.js /api/contact + nodemailer) ─────────────────
# Copy secrets to .env.local — never commit SMTP_PASS.
# NEXT_PUBLIC_CONTACT_SUBMIT_MODE=next
#
SMTP_HOST=apimstec.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no-reply@apimstec.com
SMTP_PASS=
CONTACT_MAIL_TO=info@apimstec.com
CONTACT_MAIL_FROM=no-reply@apimstec.com
# If the server uses a self-signed TLS cert (some panels show “untrusted certificate”):
# SMTP_TLS_REJECT_UNAUTHORIZED=false

# Server-only: must match FRONTEND_SEO_SYNC_SECRET in CMS (POST /api/cms-seo-sync / cms-seo-sync.php)
# CMS_SEO_SYNC_SECRET=
